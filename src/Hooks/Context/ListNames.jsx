import React from "react";
import Tables from "./Tables";

const ListNames = () => {
  return (
    <div className="list-names-container">
      <h2 className="list-names-title">List of Names</h2>
      <Tables />
    </div>
  );
};

export default ListNames;
